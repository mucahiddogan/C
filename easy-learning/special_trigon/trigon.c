#include <stdio.h>
int main (void)
{
	int a, b, c;
	
	printf ("Enter the 3 side of a trigon: \n");
	scanf ("%d%d%d", &a, &b, &c);
	
	if (a + b >= c)
	if (a*a + b*b == c*c)

	printf ("%d %d %d is a special trigon", a, b, c);
	 
	else 
	printf ("%d %d %d is not a special trigon", a, b, c);
	return 0;
}
